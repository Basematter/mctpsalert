org.gradle.jvmargs=-Xmx2G -XX:+UseG1GC
org.gradle.daemon=false

# 注意：构建需要 JDK 21。如果你本机不是 JDK 21，可用 foojay 工具链自动下载（见 settings.gradle）。
# Gradle 守护进程自身的 JVM 建议也指向 JDK 21（设置 JAVA_HOME），否则新版 JDK 可能无法启动 Gradle。
